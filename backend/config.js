//TODO: kendi database bilgilerini gir

const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '%HQznV8*',
        database: 'myFirstTrial'
    }
}

module.exports = config;